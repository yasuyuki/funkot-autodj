[workspace]
members = ["funkot-core", "funkot-cli"]
resolver = "2"

[workspace.package]
version = "0.1.0"
edition = "2021"
license = "MIT"

[profile.release]
lto = "thin"

# Analysis/DSP is far too slow unoptimized; keep dev builds usable.
[profile.dev.package."*"]
opt-level = 2
